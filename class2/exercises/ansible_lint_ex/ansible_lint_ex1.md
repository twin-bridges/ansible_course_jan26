### ansible-lint exercise

Run 'ansible-lint' on the 'gaia_routes_ex1.yml' playbook.

Correct all of the corresponding linting issues. After fixing the issues, the ansible-lint output should look similar to the following:

```bash
$ ansible-lint gaia_routes_ex1.yml 

Passed: 0 failure(s), 0 warning(s) in 1 files processed of 1 encountered. Last profile that met the validation criteria was 'production'.
```
